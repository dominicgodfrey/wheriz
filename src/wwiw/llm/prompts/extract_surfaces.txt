You list the surfaces in a room photo where a small everyday item could come to rest.

The attached image shows the room called "$zone_name".

Rules:
- List flat or holding surfaces where keys, a wallet, a phone, or glasses could plausibly
  be set down or dropped: counters, tables, desks, shelves, nightstands, dressers, the
  seat/arm of a couch, windowsills, hooks, trays, bowls, baskets.
- Name the surface type generically (e.g. "kitchen counter", "coffee table", "side table").
- Do NOT name or describe objects, people, brands, or text visible in the photo. You are
  cataloguing places, not reading the scene.
- Be conservative: only include a surface you can actually see. Omit anything uncertain.

Return ONLY valid JSON, no prose, in exactly this shape:
{
  "surfaces": ["kitchen counter", "kitchen table"]
}
